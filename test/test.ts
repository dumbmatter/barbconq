/// <reference path="../typings/mocha/mocha.d.ts" />

describe('User Model Unit Tests:', () => {

    describe('2 + 4', () => {
        it('should be 6', (done) => {
            done();
        });

        it('should not be 7', (done) => {
            done();
        });
    });
});